@model List<ShopOnline.Models.ProductViewModel>
@foreach (var product in Model)
{
    <div class="col-md-4 col-sm-6">
        <div class="product">
            <div class="image" style="height: 262px;">
                <a href="@Url.Action("Detail", "ProductList", new { id = product.ProductId })">
                    <img src="@Url.Content(product.ProductImageDisplay.ProductImageUrl)" alt="" class="img-responsive">
                </a>
            </div>
            <!-- /.image -->
            <div class="text">
                <h3><a href="@Url.Action("Detail", "ProductList", new { id = product.ProductId })">@product.ProductName</a></h3>
                <p class="price">@product.Price</p>
                <p class="buttons">
                    <a href="@Url.Action("Detail", "ProductList", new { id = product.ProductId })" class="btn btn-default">View detail</a>
                    <a href="@Url.Action("Detail", "ProductList", new { id = product.ProductId })" class="btn btn-template-main"><i class="fa fa-shopping-cart"></i>Add to cart</a>
                </p>
            </div>
            <!-- /.text -->
        </div>
        <!-- /.product -->
    </div>
}